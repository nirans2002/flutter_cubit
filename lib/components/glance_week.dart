import 'package:flutter/material.dart';

class WeekGlanceCard extends StatelessWidget {
  const WeekGlanceCard({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text("week at a glance"),
    );
  }
}
